const router=require('express').Router(); const {asyncHandler}=require('../utils/asyncHandler'); const {requireAuth}=require('../middleware/authJwt'); const ctl=require('../controllers/dashboard.controller'); router.use(requireAuth); router.get('/bajo-stock',asyncHandler(ctl.bajoStock)); router.get('/top-vendidos',asyncHandler(ctl.topVendidos)); router.get('/widget-inventario',asyncHandler(ctl.widgetInventario)); router.get('/ping',(_req,res)=>res.json({ok:true})); module.exports=router;
