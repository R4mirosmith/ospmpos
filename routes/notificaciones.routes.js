const router=require('express').Router(); const {asyncHandler}=require('../utils/asyncHandler'); const {requireAuth,requireRoles}=require('../middleware/authJwt'); const ctl=require('../controllers/notificaciones.controller'); router.use(requireAuth,requireRoles('ADMIN')); router.get('/config',asyncHandler(ctl.listarConfig)); router.post('/config',asyncHandler(ctl.guardarConfig)); router.patch('/config/:id/estado',asyncHandler(ctl.estadoConfig)); module.exports=router;
