const express=require('express'); const path=require('path'); const fs=require('fs'); const multer=require('multer');
const router=express.Router(); const {asyncHandler}=require('../utils/asyncHandler'); const {requireAuth,requireRoles}=require('../middleware/authJwt'); const ctl=require('../controllers/productos.controller');
const uploadDir=path.resolve(process.env.UPLOAD_DIR||'uploads','products'); fs.mkdirSync(uploadDir,{recursive:true});
const storage=multer.diskStorage({ destination:(req,_file,cb)=>{ const dir=path.join(uploadDir,String(req.params.id)); fs.mkdirSync(dir,{recursive:true}); cb(null,dir); }, filename:(_req,file,cb)=>cb(null,`${Date.now()}-${path.basename(file.originalname).replace(/[^a-zA-Z0-9._-]/g,'_')}`)});
const upload=multer({storage});
router.get('/:id/imagenes/public/:filename',(req,res)=>{ const safe=path.basename(req.params.filename); const file=path.join(uploadDir,String(req.params.id),safe); if(!file.startsWith(uploadDir)) return res.status(400).json({success:0}); fs.access(file,fs.constants.R_OK,(err)=> err ? res.status(404).json({success:0,status:'NOT_FOUND',result:{message:'Imagen no encontrada'}}) : res.sendFile(file)); });
router.get('/web/catalogo', asyncHandler(ctl.webCatalogo)); router.get('/web/productos/:id', asyncHandler(ctl.webProducto));
router.use(requireAuth); router.get('/',asyncHandler(ctl.listar)); router.post('/',requireRoles('ADMIN','VENDEDOR'),asyncHandler(ctl.crear)); router.get('/:id',asyncHandler(ctl.get)); router.patch('/:id',requireRoles('ADMIN','VENDEDOR'),asyncHandler(ctl.actualizar)); router.patch('/:id/estado',requireRoles('ADMIN'),asyncHandler(ctl.estado));
router.post('/:id/imagenes',requireRoles('ADMIN','VENDEDOR'),asyncHandler(ctl.imgAgregar)); router.get('/:id/imagenes',asyncHandler(ctl.imgListar)); router.patch('/:id/imagenes/:imgId/principal',requireRoles('ADMIN','VENDEDOR'),asyncHandler(ctl.imgSetPrincipal)); router.post('/:id/imagenes/:imgId/principal',requireRoles('ADMIN','VENDEDOR'),asyncHandler(ctl.imgSetPrincipal)); router.patch('/imagenes/:imgId/orden',requireRoles('ADMIN','VENDEDOR'),asyncHandler(ctl.imgReordenar)); router.post('/imagenes/:imgId/orden',requireRoles('ADMIN','VENDEDOR'),asyncHandler(ctl.imgReordenar)); router.delete('/imagenes/:imgId',requireRoles('ADMIN','VENDEDOR'),asyncHandler(ctl.imgEliminar)); router.post('/:id/imagenes/upload',requireRoles('ADMIN','VENDEDOR'),upload.array('images',3),asyncHandler(ctl.imgUpload));
module.exports=router;
