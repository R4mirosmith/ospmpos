const { pool } = require('../utils/sql');
const { ok, created, badRequest } = require('../utils/http');
async function listarConfig(req,res){ const [items]=await pool.query(`SELECT nc.*,u.nombre usuario_nombre,u.email,s.nombre sede_nombre,e.nombre empresa_nombre FROM notificacion_config nc JOIN usuario u ON u.id=nc.usuario_id JOIN sede s ON s.id=nc.sede_id JOIN empresa e ON e.id=s.empresa_id WHERE (? IS NULL OR nc.sede_id=?) ORDER BY e.nombre,s.nombre,u.nombre`,[req.query.sede_id||null,req.query.sede_id||null]); ok(res,{items,total:items.length}); }
async function guardarConfig(req,res){ const {sede_id,usuario_id,tipo='PEDIDO_WEB',activo=1}=req.body||{}; if(!sede_id||!usuario_id) return badRequest(res,'sede_id y usuario_id requeridos'); const [[sede]]=await pool.query(`SELECT empresa_id FROM sede WHERE id=?`,[sede_id]); const [r]=await pool.query(`INSERT INTO notificacion_config(empresa_id,sede_id,usuario_id,tipo,activo,fecha_creacion) VALUES(?,?,?,?,?,UTC_TIMESTAMP()) ON DUPLICATE KEY UPDATE activo=VALUES(activo)`,[sede.empresa_id,sede_id,usuario_id,tipo,activo?1:0]); created(res,{id:r.insertId||null,message:'Configuración guardada'}); }
async function estadoConfig(req,res){ await pool.query(`UPDATE notificacion_config SET activo=? WHERE id=?`,[req.body?.activo?1:0,Number(req.params.id)]); ok(res,{id:Number(req.params.id)}); }
module.exports={listarConfig,guardarConfig,estadoConfig};
